<% if book.errors.any? %>
  <div class="alert alert-warning">
    <ul>
    	<li><%= book.errors.count %> errors prohibited this obj from being saved:</li>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
